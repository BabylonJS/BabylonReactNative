/**
 * @format
 */

import {AppRegistry} from 'react-native';
import App from '@babylonjs/playground-shared/App';
import {name as appName} from '@babylonjs/playground-shared/app.json';

AppRegistry.registerComponent(appName, () => App);
