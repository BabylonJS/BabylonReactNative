{
  "description": "This package.json exists only so we can restore the tools needed to build the actual @babylonjs/react-native binary package.",
  "devDependencies": {
    "chalk": "^4.1.1",
    "fancy-log": "^1.3.3",
    "glob": "^7.1.7",
    "gulp": "^4.0.2",
    "gulp-rename": "2.0.0",
    "shelljs": "^0.8.4"
  }
}
